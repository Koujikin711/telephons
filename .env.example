# Пин для входа в систему (оставьте пустым — без авторизации)
STORE_PIN=

# Простой интерфейс: 1 = только касса/продажи/склад (по умолчанию), 0 = полный ERP
SIMPLE_UI=1

# Порт (Amvera задаёт PORT автоматически)
PORT=80
